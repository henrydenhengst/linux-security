---
- name: Deploy Full LCH Stack on Void Linux (Mattermost)
  hosts: localhost
  become: yes
  gather_facts: yes

  vars_files:
    - vars.yml

  tasks:
    # -------------------------------------------------------------------------
    # 1. VOID SYSTEM & REPO PREPARATION
    # -------------------------------------------------------------------------
    - name: Install Core System Packages
      xbps:
        name:
          - podman
          - curl
          - git
          - python3
          - dbus
          - sudo
          - ip6tables
          - iptables
          - dcron
          - syslog-ng
          - fail2ban
          - zram-enabler
          - logrotate
        state: present
        update: yes

    - name: Ensure podman user exists
      user:
        name: "{{ podman_user }}"
        shell: /bin/sh
        state: present
        create_home: yes
        uid: "{{ container_user_uid }}"
        groups: wheel

    - name: Configure Rootless Mapping
      copy:
        dest: "{{ item }}"
        content: "{{ podman_user }}:100000:65536\n"
      loop:
        - /etc/subuid
        - /etc/subgid

    # -------------------------------------------------------------------------
    # 2. SECURITY: KERNEL HARDENING & FIREWALL
    # -------------------------------------------------------------------------
    - name: Kernel Hardening (Anti-DoS & Spoofing)
      sysctl:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        state: present
        sysctl_file: /etc/sysctl.d/99-security.conf
      loop:
        - { name: 'net.ipv4.tcp_syncookies', value: '1' }
        - { name: 'net.ipv4.conf.all.log_martians', value: '1' }
        - { name: 'net.ipv4.icmp_echo_ignore_broadcasts', value: '1' }
        - { name: 'net.ipv4.conf.all.accept_source_route', value: '0' }
        - { name: 'net.ipv6.conf.all.disable_ipv6', value: '1' }

    - name: Configure Firewall Rules (IPv4)
      shell: |
        iptables -F
        iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
        iptables -A INPUT -i lo -j ACCEPT
        iptables -A INPUT -p tcp --dport 22 -j ACCEPT
        iptables -A INPUT -p tcp --dport 80 -j ACCEPT
        iptables -A INPUT -p tcp --dport 443 -j ACCEPT
        iptables -P INPUT DROP
        iptables -P FORWARD DROP
        iptables-save > /etc/iptables.rules

    - name: Ensure Firewall persists after reboot
      copy:
        dest: /etc/rc.local
        content: |
          #!/bin/sh
          iptables-restore < /etc/iptables.rules
          exit 0
        mode: '0755'

    - name: SSH Hardening - Disable Passwords
      lineinfile:
        path: /etc/ssh/sshd_config
        regexp: "{{ item.regexp }}"
        line: "{{ item.line }}"
      loop:
        - { regexp: '^#?PermitRootLogin', line: 'PermitRootLogin no' }
        - { regexp: '^#?PasswordAuthentication', line: 'PasswordAuthentication no' }
      notify: Restart SSH

    - name: Configure Fail2ban Jail & Logrotate
      copy:
        dest: "{{ item.dest }}"
        content: "{{ item.content }}"
      loop:
        - { dest: '/etc/fail2ban/jail.local', content: "[sshd]\nenabled = true\nfilter = sshd\nport = 22\nlogpath = /var/log/messages\nmaxretry = 3\nbantime = 1d" }
        - { dest: '/etc/logrotate.d/syslog', content: "/var/log/messages /var/log/auth.log {\n    weekly\n    rotate 4\n    compress\n    delaycompress\n    missingok\n    notifempty\n}" }

    # -------------------------------------------------------------------------
    # 3. DIRECTORY STRUCTURE
    # -------------------------------------------------------------------------
    - name: Create App Directories
      file:
        path: "/home/{{ podman_user }}/containers/{{ item }}"
        state: directory
        owner: "{{ podman_user }}"
        group: "{{ podman_user }}"
        mode: '0755'
      loop: [caddy/data, caddy/config, nginx/html, mattermost/data, mattermost/logs, postgres/data, backups]

    # -------------------------------------------------------------------------
    # 4. CONFIGURATION FILES
    # -------------------------------------------------------------------------
    - name: Create Caddyfile
      copy:
        dest: "/home/{{ podman_user }}/containers/caddy/Caddyfile"
        content: |
          {{ domain_name }} {
              reverse_proxy nginx:80
          }
          chat.{{ domain_name }} {
              reverse_proxy mattermost-app:8065
          }
        owner: "{{ podman_user }}"

    # -------------------------------------------------------------------------
    # 5. NETWORK & CONTAINERS
    # -------------------------------------------------------------------------
    - name: Create Podman Network
      become_user: "{{ podman_user }}"
      command: podman network exists lchnet || podman network create lchnet

    - name: Deploy PostgreSQL (Mattermost DB)
      become_user: "{{ podman_user }}"
      containers.podman.podman_container:
        name: mattermost-db
        image: "docker.io/library/postgres:{{ postgres_version }}"
        state: started
        restart_policy: always
        network: lchnet
        env:
          POSTGRES_USER: "{{ db_user }}"
          POSTGRES_PASSWORD: "{{ db_password }}"
          POSTGRES_DB: "{{ db_name }}"
        volumes:
          - "/home/{{ podman_user }}/containers/postgres/data:/var/lib/postgresql/data:Z"

    - name: Deploy Mattermost App
      become_user: "{{ podman_user }}"
      containers.podman.podman_container:
        name: mattermost-app
        image: "docker.io/mattermost/mattermost-team-edition:{{ mattermost_version }}"
        state: started
        restart_policy: always
        network: lchnet
        env:
          MM_SQLSETTINGS_DRIVERNAME: "postgres"
          MM_SQLSETTINGS_DATASOURCE: "postgres://{{ db_user }}:{{ db_password }}@mattermost-db:5432/{{ db_name }}?sslmode=disable&connect_timeout=10"
          MM_SERVICESETTINGS_SITEURL: "https://chat.{{ domain_name }}"
        volumes:
          - "/home/{{ podman_user }}/containers/mattermost/data:/mattermost/data:Z"
          - "/home/{{ podman_user }}/containers/mattermost/logs:/mattermost/logs:Z"
        labels:
          io.containers.autoupdate: "image"

    - name: Deploy Web Proxy (Caddy & Nginx)
      become_user: "{{ podman_user }}"
      containers.podman.podman_container:
        name: "{{ item.name }}"
        image: "{{ item.image }}"
        state: started
        restart_policy: always
        network: lchnet
        ports: "{{ item.ports | default(omit) }}"
        volumes: "{{ item.volumes | default(omit) }}"
      loop:
        - { name: 'nginx', image: "docker.io/nginx:{{ nginx_version }}", volumes: ["/home/{{ podman_user }}/containers/nginx/html:/usr/share/nginx/html:Z"] }
        - { name: 'caddy', image: "docker.io/caddy:{{ caddy_version }}", ports: ["80:80", "443:443"], volumes: ["/home/{{ podman_user }}/containers/caddy/Caddyfile:/etc/caddy/Caddyfile:Z", "/home/{{ podman_user }}/containers/caddy/data:/data:Z"] }

    # -------------------------------------------------------------------------
    # 6. PERSISTENCE & AUTO-UPDATES
    # -------------------------------------------------------------------------
    - name: Create Runit service voor auto-start containers
      file:
        path: /etc/sv/podman-lch
        state: directory

    - name: Create Runit Run Script
      copy:
        dest: /etc/sv/podman-lch/run
        mode: '0755'
        content: |
          #!/bin/sh
          su - {{ podman_user }} -c 'podman start --all'
          exec pause

    - name: Enable Runit Services
      file:
        src: "/etc/sv/{{ item }}"
        dest: "/var/service/{{ item }}"
        state: link
      loop: [podman-lch, dbus, dcron, syslog-ng, fail2ban, zram-enabler]

    - name: Setup Cronjobs
      cron:
        name: "{{ item.name }}"
        user: "{{ item.user }}"
        minute: "0"
        hour: "{{ item.hour }}"
        job: "{{ item.job }}"
      loop:
        - { name: "System Updates", user: "root", hour: "3", job: "xbps-install -Syu -y && podman image prune -f" }
        - { name: "Mattermost DB Backup", user: "root", hour: "2", job: "podman exec mattermost-db pg_dump -U {{ db_user }} {{ db_name }} > /home/{{ podman_user }}/backups/mm_$(date +%F).sql" }
        - { name: "Container Auto-update", user: "{{ podman_user }}", hour: "4", job: "podman auto-update" }

  handlers:
    - name: Restart SSH
      shell: "sv restart sshd"
